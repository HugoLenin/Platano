# Emergency Language Bridge - Python agent
# Pinned to the 1.7.x line of livekit-agents (current as of 2026-08).
livekit-agents[deepgram,anthropic,elevenlabs,silero,turn-detector]~=1.7
livekit-api~=1.0
anthropic>=1.0,<2
pydantic>=2.9
httpx>=0.27
python-dotenv>=1.0

# dev
pytest>=8.0
pytest-asyncio>=0.24

org.gradle.jvmargs=-Xmx3072m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
# Disabled: AGP 9.3's ProcessNavigationXmlTask cannot be serialised into the
# configuration cache ("error writing value of type DefaultConfigurableFileCollection").
# Re-enable once AGP fixes it; it only affects build speed, not the artifact.
org.gradle.configuration-cache=false

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

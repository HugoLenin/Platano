<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!--
      ACCESS_BACKGROUND_LOCATION is deliberately NOT declared.

      On Android 11+ it cannot be requested in the same dialog as foreground
      location: the system dialog drops "Allow all the time" and the user has
      to be sent to a settings page in a separate, second step. That is a real
      constraint, and the standard workaround for it is worse than the problem.

      We avoid needing it at all. Location is only read while an emergency call
      is active, and the call runs inside a foreground service typed
      `microphone|location`. A foreground service with the location type may
      access location while the app is backgrounded or the screen is locked,
      WITHOUT the background permission. See docs/DECISIONS.md.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <uses-feature android:name="android.hardware.microphone" android:required="true" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:name=".ElbApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Elb"
        android:usesCleartextTraffic="true">
        <!--
          usesCleartextTraffic is enabled so the APK can talk to a laptop
          running `npm run dev` over plain HTTP on the venue wifi. Remove it
          for anything resembling production.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:theme="@style/Theme.Elb">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".call.CallService"
            android:exported="false"
            android:foregroundServiceType="microphone|location" />
    </application>
</manifest>
